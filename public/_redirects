# The function route must come before the SPA catch-all, or the catch-all
# swallows it and the signup gets index.html instead of the function.
/api/subscribe  /.netlify/functions/subscribe  200

/*  /index.html  200
